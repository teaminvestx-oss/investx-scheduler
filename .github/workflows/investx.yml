name: InvestX Telegram Scheduler

on:
  schedule:
    # 09:00 Madrid
    - cron: "0 7 * * 1-5"     # CEST 09:00 (verano)
    - cron: "0 8 * * 1-5"     # CET  09:00 (invierno)
    # 10:30 Madrid
    - cron: "30 8 * * 1-5"    # CEST 10:30 (verano)
    - cron: "30 9 * * 1-5"    # CET  10:30 (invierno)
  workflow_dispatch:
    inputs:
      slot:
        description: "Forzar franja: morning | tip | auto"
        required: false
        default: "auto"

concurrency:
  group: investx-telegram-scheduler
  cancel-in-progress: false

jobs:
  send:
    runs-on: ubuntu-latest
    env:
      BOT_TOKEN: ${{ secrets.INVESTX_TOKEN }}
      CHAT_ID:   ${{ secrets.CHAT_ID }}
    steps:
      - name: Enviar mensaje (rotación temática)
        env:
          TZ: Europe/Madrid
        run: |
          set -euo pipefail

          # --------- Fecha/hora ---------
          DOW="$(date +%u)"          # 1=L ... 7=D
          HHMM_NOW="$(date +'%H:%M')"
          DOY="$(date +%j)"          # 001..366
          SLOT="${{ github.event.inputs.slot || 'auto' }}"

          # --------- Fin de semana ---------
          if [ "$DOW" -gt 5 ] && [ "$SLOT" = "auto" ]; then
            echo "Fin de semana: no se envía."
            exit 0
          fi

          # --------- Telegram helper ---------
          send() {
            local text="$1"
            curl -sS "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
              -d chat_id="${CHAT_ID}" -d parse_mode="HTML" \
              --data-urlencode text="${text}" >/dev/null
          }

          # =========================
          # VARIANTES TEMÁTICAS
          # =========================

          # 09:00 — Buenos días (10)
          MORNING_VARS=(
            $'🌞 <b>¡Buenos días, equipo!</b>\n\nArrancamos la sesión con el mapa listo: soportes, resistencias y catalizadores marcados. Hoy el foco está en <b>ejecutar limpio</b>.'
            $'☕ <b>Buenos días, InvestX.</b>\n\nEmpieza la jornada con setups claros y un recordatorio: <b>paciencia > impulsividad</b>. Espera que el precio venga a ti.'
            $'🌅 <b>Buenos días.</b>\n\nJornada nueva, mismas reglas: contexto, confluencia y plan. El institucional ya ha dejado sus huellas, toca seguirlas.'
            $'🚀 <b>Arrancamos el día.</b>\n\nRevisa tu lista A/B, define sesgo y RR mínimo. La consistencia empieza en el checklist de la mañana.'
            $'🔔 <b>Buenos días, traders.</b>\n\nHoy mandan las estructuras de valor: detecta las zonas donde el volumen se concentra y filtra el ruido.'
            $'📈 <b>InvestX en marcha.</b>\n\nPrepárate: momentum, liquidez y catalizadores. Menos operaciones, más calidad.'
            $'💡 <b>Buenos días.</b>\n\nHoy toca recordar que la ventaja está en la <b>planificación previa</b>: niveles y escenarios definidos antes de abrir el mercado.'
            $'🧭 <b>Arranque de jornada.</b>\n\nEntrada justificada, salida planificada. Esa es la fórmula. Sin plan, no hay trade.'
            $'🔍 <b>Buenos días.</b>\n\nEmpieza con visión macro y baja a micro: cuando ambos marcos cuentan la misma historia, la probabilidad se multiplica.'
            $'🧠 <b>Buenos días, equipo.</b>\n\nObserva primero, reacciona después. El mercado recompensa la paciencia y castiga la prisa.'
          )

          # 10:30 — Tip del canal (#ticker) (10)
          TIP_VARS=(
            $'📌 <b>TIP del canal.</b>\n\nPulsa un <b>#ticker</b> y verás todas las menciones, informes y setups de ese activo. Contexto al instante, mejor decisión.'
            $'🧠 <b>TIP del canal.</b>\n\nAntes de entrar en una operación, revisa el <b>#ticker</b>. ¿Catalizadores hoy? ¿Qué opinaba el canal ayer? Te ahorra errores.'
            $'🔍 <b>TIP del canal.</b>\n\nEl <b>#ticker</b> agrupa ideas, resúmenes y alertas de cada activo. Úsalo como repaso rápido antes de abrir posición.'
            $'⚙️ <b>TIP del canal.</b>\n\nCrea rutina: señal → revisa el <b>#ticker</b> → valida contexto. Así filtras operaciones débiles.'
            $'🧭 <b>TIP del canal.</b>\n\nSi un activo está en tu lista A/B, pásate por su <b>#ticker</b>: verás niveles defendidos y setups vivos.'
            $'📎 <b>TIP del canal.</b>\n\nGuarda mensajes clave y luego accede con <b>#ticker</b>. Mantienes coherencia en tu plan.'
            $'📚 <b>TIP del canal.</b>\n\nCada <b>#ticker</b> es un mini-historial del activo: zonas, noticias y análisis. No lo ignores antes de tomar decisión.'
            $'🧩 <b>TIP del canal.</b>\n\nCon el <b>#ticker</b> reduces ruido: toda la info en un clic. Contexto es ventaja.'
            $'🛰️ <b>TIP del canal.</b>\n\nUsa el <b>#ticker</b> para unir visión macro y micro del mismo valor. Panorama completo en segundos.'
            $'🗂️ <b>TIP del canal.</b>\n\nTu flujo debería ser: idea → <b>#ticker</b> → validar → ejecutar. Proceso repetible = consistencia.'
          )

          # --------- Determinar SLOT si auto ---------
          if [ "$SLOT" = "auto" ]; then
            case "$HHMM_NOW" in
              "09:00") SLOT="morning";;
              "10:30") SLOT="tip";;
              *) echo "Auto: ${HHMM_NOW} no tiene mensaje. No se envía."; exit 0;;
            esac
          fi

          # --------- Índice por franja (rotación por día) ---------
          pick_idx() { local size="$1"; local doy="$2"; echo $(( (10#${doy} - 1) % size )); }

          case "$SLOT" in
            morning) i="$(pick_idx ${#MORNING_VARS[@]} "$DOY")"; TEXTO="${MORNING_VARS[$i]}";;
            tip)     i="$(pick_idx ${#TIP_VARS[@]} "$DOY")";     TEXTO="${TIP_VARS[$i]}";;
            *) echo "slot inválido: $SLOT"; exit 1;;
          esac

          echo "SLOT=${SLOT} | DOY=${DOY} | VAR=${i} | HHMM_NOW=${HHMM_NOW}"
          send "${TEXTO}"
