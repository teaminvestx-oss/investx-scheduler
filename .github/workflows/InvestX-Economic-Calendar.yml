name: InvestX Economic Calendar

on:
  schedule:
    - cron: "0 11 * * 1"   # Lunes 13:00 Madrid (11:00 UTC) — semana completa
    - cron: "0 11 * * 2-5" # Mar–Vie 13:00 Madrid (11:00 UTC) — solo hoy
  workflow_dispatch:

concurrency:
  group: investx-econ-calendar
  cancel-in-progress: false

jobs:
  econ-calendar:
    runs-on: ubuntu-22.04
    timeout-minutes: 20

    steps:
      - name: System deps
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq curl unzip python3 python3-pip \
            libatk1.0-0 libatk-bridge2.0-0 libnss3 libx11-6 libx11-xcb1 \
            libxcomposite1 libxdamage1 libxext6 libxfixes3 libxrandr2 \
            libgbm1 libasound2 fonts-liberation
          python3 -m pip install --upgrade pip requests

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          check-latest: true

      - name: Install Puppeteer
        run: |
          test -f package.json || npm init -y
          npm i puppeteer

      # ---------------- SCRAPER ----------------
      - name: Build scraper (PNG + JSON)
        run: |
          cat > grab.js <<'EOF'
          const puppeteer = require('puppeteer');
          const fs = require('fs');
          const wait = (ms)=>new Promise(r=>setTimeout(r,ms));

          async function clickTxt(p, sel, keys, ms=8000){
            const end = Date.now()+ms, wants = keys.map(x=>x.toLowerCase());
            while(Date.now()<end){
              const ok = await p.evaluate(({sel,wants})=>{
                for(const el of document.querySelectorAll(sel)){
                  const t = ((el.innerText||el.textContent||el.value||'')+'').toLowerCase().trim();
                  if(wants.some(w=>t.includes(w))){ el.click(); return true; }
                } return false;
              }, {sel, wants});
              if(ok) return true;
              await wait(250);
            }
            return false;
          }

          async function applyFilters(p){
            await clickTxt(p, 'button,a,[role="button"],input[type="button"]', ['filtro','filtros','filters'], 8000);
            await wait(600);
            await p.evaluate(()=>{
              const m = document.querySelector('.filterPopup,[class*="filterPop"]') || document;
              const cs = [...m.querySelectorAll('input[type="checkbox"][name*="country"]')];
              cs.forEach(cb=>{ cb.checked=false; cb.dispatchEvent(new Event('change',{bubbles:true})) });
              let us = cs.find(cb=>cb.value==='5') || cs.find(cb=>/estados unidos|united states|ee\.uu/i.test((cb.closest('label,li,div')?.innerText||'')));
              if(us){ us.checked=true; us.dispatchEvent(new Event('change',{bubbles:true})) }
              const imp = [...m.querySelectorAll('input[type="checkbox"][name*="importance"]')];
              imp.forEach(cb=>{ cb.checked=(cb.value==='2'||cb.value==='3'); cb.dispatchEvent(new Event('change',{bubbles:true})) });
              const btn = [...m.querySelectorAll('button,a,input[type="submit"],input[type="button"]')].find(b=>/aplicar|apply|mostrar|show/i.test((b.innerText||b.value||'')));
              btn?.click();
            });
            await wait(1200);
          }

          (async()=>{
            const browser = await puppeteer.launch({ headless:'new', args:['--no-sandbox','--disable-setuid-sandbox','--lang=es-ES,es'] });
            const page = await browser.newPage();
            await page.setUserAgent('Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124 Safari/537.36');
            await page.setExtraHTTPHeaders({'Accept-Language':'es-ES,es;q=0.9'});
            await page.setViewport({width:1440, height:2400, deviceScaleFactor:2});

            await page.goto('https://es.investing.com/economic-calendar/', {waitUntil:'domcontentloaded', timeout:60000});
            await page.evaluate(()=>{ const b=[...document.querySelectorAll("button,a,[role='button']")].find(x=>/aceptar|accept|consent|agree/i.test((x.innerText||''))); b?.click(); }).catch(()=>{});
            await wait(600);

            const dow = new Date().getDay();
            await clickTxt(page, 'a,button', dow===1?['esta semana','this week']:['hoy','today'], 6000);
            await wait(500);
            await applyFilters(page);
            await wait(600);

            // Limpieza y extracción: guardamos TODOS los eventos visibles
            const data = await page.evaluate(()=>{
              const rows = [...document.querySelectorAll('tr[id^="eventRowId_"],tr.js-event-item,tr[data-event-datetime]')];

              const cleanTitle = (tr)=>{
                const pick = (...xs)=>xs.find(v=>v && v.trim && v.trim().length>0);
                const byAttr = tr.getAttribute('data-event-title');
                const aTitle = tr.querySelector('td[class*="event"] a[title]')?.getAttribute('title');
                const aria   = tr.querySelector('td[class*="event"] [aria-label]')?.getAttribute('aria-label');
                const txt1   = tr.querySelector('td[class*="event"] a')?.textContent;
                const txt2   = tr.querySelector('td[class*="event"], td.left, td:nth-child(3)')?.textContent;
                let raw = (pick(byAttr,aTitle,aria,txt1,txt2) || '').replace(/\s+/g,' ').trim();
                if (/^\d{1,2}:\d{2}$/.test(raw) || raw.length < 4) raw = '';
                return raw;
              };

              const events = [];
              for(const tr of rows){
                if (tr.style.display==='none') continue; // respeta filtro UI (USA + 2/3⭐)
                const tds = tr.querySelectorAll('td');
                const time = (tds[0]?.innerText||'').trim(); // HH:MM de la web
                const title = cleanTitle(tr);
                if(!title) continue;

                let imp = parseInt(tr.getAttribute('data-importance')||'0',10);
                if(!imp||isNaN(imp)){
                  const s = tr.querySelector('td.sentiment,td.impact,.sentiment');
                  if (s) {
                    const n = s.querySelectorAll('i,svg').length;
                    imp = n>=3?3:(n>=2?2:1);
                  } else { imp = 0; }
                }

                events.push({
                  time,
                  title,
                  importance: imp,
                  iso: tr.getAttribute('data-event-datetime') || '',
                  forecast: (tr.querySelector('td.fore,td.forecast')?.innerText||'').trim(),
                  previous: (tr.querySelector('td.prev,td.previous')?.innerText||'').trim()
                });
              }
              return { events };
            });

            // Captura
            const table = await page.$('#economicCalendarData') || await page.$('table.genTbl');
            if (table) { await table.screenshot({path:'calendar.png'}); }

            require('fs').writeFileSync('calendar.json', JSON.stringify(data, null, 2));
            await browser.close();
            if (!data.events.length) process.exit(0);
          })().catch(e=>{ console.error(e); process.exit(1); });
          EOF

          node grab.js

      # ---------------- CAPTION FECHA ----------------
      - name: Build caption (ES/Madrid)
        if: ${{ hashFiles('calendar.png') != '' }}
        run: |
          export TZ=Europe/Madrid
          if [ "$(date +%u)" -eq 1 ]; then
            echo "CAPTION=🗓️ Calendario USA (⭐️⭐️/⭐️⭐️⭐️) — Semana $(date -d 'monday this week' +%Y-%m-%d)–$(date -d 'sunday this week' +%Y-%m-%d)" >> $GITHUB_ENV
          else
            echo "CAPTION=🗓️ Calendario USA (⭐️⭐️/⭐️⭐️⭐️) — Hoy $(date +%Y-%m-%d)" >> $GITHUB_ENV
          fi

      # ---------------- RESUMEN ANALISTA ----------------
      - name: Build summary (párrafos 3–5 líneas con fallback)
        if: ${{ hashFiles('calendar.json') != '' }}
        run: |
          python3 - <<'PY'
          import json, re, unicodedata
          from datetime import datetime
          from zoneinfo import ZoneInfo

          tz = ZoneInfo("Europe/Madrid")
          data = json.load(open('calendar.json', encoding='utf-8'))
          events = data.get('events', [])

          def norm(s):
            s = unicodedata.normalize('NFKD', s or '').encode('ascii','ignore').decode()
            return re.sub(r'\s+',' ', s or '').strip().lower()

          # Tipo + score
          def kind_and_score(title, imp):
            t = norm(title)
            if any(k in t for k in ['fed','fomc','powell']):      return ('FOMC', 100+imp)
            if any(k in t for k in ['ipc','cpi']):                return ('CPI',    95+imp)
            if 'pce' in t:                                        return ('PCE',    92+imp)
            if any(k in t for k in ['nóminas','nominas','empleo','nfp','desempleo']): return ('NFP', 90+imp)
            if any(k in t for k in ['pib','gdp']):                return ('GDP',    85+imp)
            if any(k in t for k in ['pmi','ism']):                return ('PMI',    80+imp)
            return (None, imp)

          # Dedup + anotación de tipo/score (NO filtramos por tipo aún)
          seen, annotated = set(), []
          for e in events:
            t = (e.get('title') or '').strip()
            if len(t) < 4: continue
            k, sc = kind_and_score(t, int(e.get('importance') or 0))
            key = norm(t)
            if key in seen: continue
            seen.add(key); e['_kind'], e['_score'] = k, sc
            annotated.append(e)

          if not annotated:
            open('summary.txt','w').close(); raise SystemExit(0)

          # Preferimos tipos estrella; si no hay, usamos los mejores 2–3 como fallback
          stars = [x for x in annotated if x['_kind'] is not None]
          base  = stars if stars else sorted(annotated, key=lambda x: -x['_score'])[:3]

          weekly = datetime.now(tz).isoweekday()==1
          take = 4 if weekly else 3
          sel = sorted(base, key=lambda x: (-x['_score'], x.get('time','')))[:take]

          def hhmm(e):
            h = (e.get('time') or '').strip()
            return h if re.match(r'^\d{1,2}:\d{2}$', h) else (h or '--:--')

          def paragraph(e):
            t = (e.get('title') or '').strip()
            k = e['_kind']
            time = hhmm(e)
            prev, fore = e.get('previous'), e.get('forecast')

            if k == 'CPI':
              head = f"📌 <b>IPC USA ({time})</b>"
              ctx  = ("La referencia clave de inflación mensual. El mercado vigilará subyacente "
                      "(servicios ‘supercore’ y alquileres). > consenso → suben Tesoros (rendimientos) y USD; "
                      "< consenso → respira la curva y apoyan índices growth.")
            elif k == 'PCE':
              head = f"📌 <b>PCE subyacente ({time})</b>"
              ctx  = ("Indicador preferido por la Fed. Desviaciones frente al consenso mueven dólar y curva; "
                      "sorpresa a la baja suele apoyar a renta variable.")
            elif k == 'FOMC':
              head = f"📌 <b>Decisión FOMC + Powell ({time})</b>"
              ctx  = ("Se valoran comunicado y tono de Powell. Hawkish → condiciones financieras más duras "
                      "(USD/yields ↑, bolsa volátil); dovish → alivio para equities y crédito.")
            elif k == 'NFP':
              head = f"📌 <b>Nóminas no agrícolas – NFP ({time})</b>"
              ctx  = ("Se miran creación de empleo, salarios/hora y participación. Fuerte + salarios altos mantiene "
                      "presión de precios (yields ↑, USD ↑); débil abre espacio a recortes y apoya índices.")
            elif k == 'GDP':
              head = f"📌 <b>PIB (GDP) ({time})</b>"
              ctx  = ("Pulso de la actividad. Lectura sólida refuerza resiliencia y puede presionar yields; "
                      "dato flojo reaviva apuestas de recortes y favorece duration.")
            elif k == 'PMI':
              head = f"📌 <b>ISM/PMI ({time})</b>"
              ctx  = ("Termómetro adelantado del ciclo: foco en nuevas órdenes y precios pagados. "
                      ">50 sostiene cíclicas y USD; <50 avisa de enfriamiento y favorece defensivas.")
            else:
              head = f"📌 <b>{t} ({time})</b>"
              ctx  = ("Catalizador potencial para USD, bonos y equity según sorpresa frente al consenso; "
                      "vigilar composición del dato y el tono de referencias cruzadas.")

            meta=[]
            if fore: meta.append(f"consenso {fore}")
            if prev: meta.append(f"anterior {prev}")
            extra = (" — " + ", ".join(meta)) if meta else ""
            return f"{head}\n{ctx}{extra}"

          lines = ["📰 <b>Resumen principales noticias</b>"]
          for e in sel:
            lines.append(paragraph(e))

          open('summary.txt','w',encoding='utf-8').write("\n\n".join(lines))
          # Debug en logs
          print("=== DEBUG selección ===")
          for e in sel:
            print(e.get('_kind'), e.get('time'), "|", e.get('title'))
          PY

      # ---------------- ENVÍO A TELEGRAM (2 MENSAJES) ----------------
      - name: Send image to Telegram
        if: ${{ hashFiles('calendar.png') != '' }}
        env:
          BOT_TOKEN: ${{ secrets.INVESTX_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          CAPTION: ${{ env.CAPTION }}
        run: |
          python3 - <<'PY'
          import os,requests
          bot=os.environ['BOT_TOKEN']; chat=os.environ['CHAT_ID']
          caption=os.environ.get('CAPTION','Calendario USA')
          with open("calendar.png","rb") as f:
            r=requests.post(
              f"https://api.telegram.org/bot{bot}/sendPhoto",
              data={"chat_id":chat,"caption":caption,"parse_mode":"HTML"},
              files={"photo":("calendar.png",f,"image/png")}, timeout=90
            ); print(r.status_code,r.text); r.raise_for_status()
          PY

      - name: Send summary to Telegram (mensaje separado)
        if: ${{ hashFiles('summary.txt') != '' }}
        env:
          BOT_TOKEN: ${{ secrets.INVESTX_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          python3 - <<'PY'
          import os,requests
          bot=os.environ['BOT_TOKEN']; chat=os.environ['CHAT_ID']
          text=open('summary.txt','r',encoding='utf-8').read().strip()
          if text:
            r=requests.post(
              f"https://api.telegram.org/bot{bot}/sendMessage",
              data={"chat_id":chat,"text":text,"parse_mode":"HTML","disable_web_page_preview":True},
              timeout=90
            ); print(r.status_code,r.text); r.raise_for_status()
          PY
